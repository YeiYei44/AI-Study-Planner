"""Textual TUI: a persistent, browsable view over the same data the CLI
commands already work with — dashboard, plan, an actual tutor chat
instead of one-shot `ask` calls, and calibration.

Login, cookie import/export, and material upload stay CLI-only — they're
one-shot browser/file operations, not naturally interactive. Launch the
TUI with `asp tui` (or `python -m app.cli tui`).
"""
