# Copy to .env and adjust. All keys are prefixed ASP_.

ASP_CANVAS_BASE_URL=https://fultonschools.instructure.com
ASP_HEADLESS=true

# Needed later by the planner/tutor, not by Canvas sync:
# ASP_ANTHROPIC_API_KEY=sk-ant-...
